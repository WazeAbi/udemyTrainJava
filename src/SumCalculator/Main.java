package SumCalculator;

public class Main {
    public static void main(String[] args) {
        Number first = new Number();
        first.setFirstNumber(10);
        first.setSecondNumber(2);
        first.getAdditionResult();
    }
}
